#include "test_class.h"
#include "test_class_template.h"

int main(){
    X x;
    x.f();          // 普通类没问题

    X2<int> x2;     // 类模板没问题
    //x2.f();         // 链接错误
}

/*为什么我们可以使用类模板呢？因为其实类模板又没有真的分声明和定义（也做不到），它是在h文件定义的
 * 所以我们可以使用类模板，但是却没办法调用它的成员函数，因为它这个的确只有声明，实现在 cpp 文件。
 */